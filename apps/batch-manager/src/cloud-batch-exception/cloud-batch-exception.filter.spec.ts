import { CloudBatchExceptionFilter } from './cloud-batch-exception.filter';

describe('CloudBatchExceptionFilter', () => {
  it('should be defined', () => {
    expect(new CloudBatchExceptionFilter()).toBeDefined();
  });
});
